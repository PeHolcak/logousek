import { Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../styles.ts'
import PropertiesTable from '../../../Components/properties-table'
import ReferenceList from '../../../Components/reference-list'
import ComponentInfoTable from '../../../Components/component-info-table'

<Meta title="ApplicationModel/Components/SimpleMenu/docs" />

# SimpleMenu

<ComponentInfoTable
  name={'SimpleMenu'}
  description={`This is a application navigation.`}
  source="components/simple-menu"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'data',
      type: `{
              name: string;
              icon: string;
              title: string;
              onClick: () => void;
            }`,
      defaultValue: '',
      description: `This is the menu configuration of the aplication menu.`,
      required: true,
    },
    {
      name: 'isMenuShow',
      type: `boolean`,
      defaultValue: '',
      description: `Indicates whether the menu is open.`,
      required: true,
    },
    {
      name: 'setIsMenuShow',
      type: `boolean`,
      defaultValue: '',
      description: `This is a callback. It is called when the menu is opened or closed.`,
      required: true,
    },
  ]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
     SimpleMenu "1" <-- "1" SimpleMenuWrapper
     SimpleMenuWrapper "1" <-- "1" H4
     
     SimpleMenuWrapper "1" <-- "1" ItemRectListWrapper
     SimpleMenuWrapper "1" <-- "1" DarkModeSwitchWrapper
     DarkModeSwitchWrapper "1" <-- "1" DarkModeSwitch

     ItemListWrapper "1" <-- "1" ItemList

     class SimpleMenu:::green
     class SimpleMenuWrapper
     class H4:::blue
     class ItemListWrapper
     class ItemList:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[
    {
      name: 'ItemList',
      description: 'It is a list of items.',
    },
    {
      name: 'H4',
      description: 'It is a Level 4 Header component',
    },
  ]}
/>
