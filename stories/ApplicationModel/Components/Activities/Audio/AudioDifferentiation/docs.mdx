{/* Checkbox.mdx */}

import { Canvas, Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../../../styles.ts'
import PropertiesTable from '../../../../../Components/properties-table'
import ReferenceList from '../../../../../Components/reference-list'
import ComponentInfoTable from '../../../../../Components/component-info-table'

<Meta title="ApplicationModel/Components/Activities/Audio/AudioDifferentiation/docs" />

# AudioDifferentiation

<ComponentInfoTable
  name={'AudioDifferentiation'}
  description={`This is a visual differentiation activity.`}
  source="components/AudioDifferentiation"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'complexity',
      type: `string`,
      defaultValue: '',
      description: `Indicates the complexity.`,
      required: true,
    },
    {
      name: 'onHandleChanged',
      type: `(canBeChecked?: boolean) => void`,
      defaultValue: '',
      description: `Callback that is called if the game state changes`,
      required: true,
    },
  ]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
      AudioDifferentiation "1" <-- "1" div
      div "1" <-- "1" SoundWrapper
      div "1" <-- "1" P52
      div "1" <-- "1" CardList

      SoundWrapper "1" <-- "1" P5
      SoundWrapper "1" <-- "1" SoundButtonWrapper
      SoundButtonWrapper "1" <-- "1" SoundButton

      class div
      class AudioDifferentiation:::green
      class SoundWrapper
      class P5:::blue
      class SoundButtonWrapper
      class SoundButton:::blue
      class P52["P5"]:::blue
      class CardList:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[
    {
      name: 'P5',
      description: 'Paragraph of level 5.',
    },
    {
      name: 'CardList',
      description: 'List of Cards.',
    },
    {
      name: 'SoundButton',
      description: 'Button that can play a sound.',
    },
  ]}
/>
