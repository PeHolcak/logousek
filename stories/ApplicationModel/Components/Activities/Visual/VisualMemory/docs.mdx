{/* Checkbox.mdx */}

import { Canvas, Meta } from '@storybook/blocks'
import { Mermaid } from 'mdx-mermaid/Mermaid'

import * as S from '../../../../styles.ts'
import PropertiesTable from '../../../../../Components/properties-table'
import ReferenceList from '../../../../../Components/reference-list'
import ComponentInfoTable from '../../../../../Components/component-info-table'

<Meta title="ApplicationModel/Components/Activities/Visual/VisualMemory/docs" />

# VisualMemory

<ComponentInfoTable
  name={'VisualMemory'}
  description={`This is a visual memory activity.`}
  source="components/VisualMemory"
/>

<PropertiesTable
  tableRows={[
    {
      name: 'complexity',
      type: `string`,
      defaultValue: '',
      description: `Indicates the complexity.`,
      required: true,
    },
    {
      name: 'onHandleChanged',
      type: `(canBeChecked?: boolean) => void`,
      defaultValue: '',
      description: `Callback that is called if the game state changes`,
      required: true,
    },
  ]}
/>

<S.MermaidWrapper>
<Mermaid
  chart={`
     classDiagram
     direction RL
      VisualMemory "1" <-- "1" ActivitySlider
      ActivitySlider "1" <-- "1" ActivityCardList
      ActivityCardList "1" <-- "N" ActivityCard

      class VisualMemory:::green
      class ActivitySlider:::blue
      class ActivityCardList
      class ActivityCard:::blue

`}
/>

</S.MermaidWrapper>

<ReferenceList
  tableRows={[
    {
      name: 'P5',
      description: 'Paragraph of level 5.',
    },
    {
      name: 'CardList',
      description: 'List of Cards for VisualCard.',
    },
  ]}
/>
