import styled from 'styled-components'

export const DailyBonusWrapper = styled.div`
  
`

export const DailyBonusList = styled.div`
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;

    padding: 16px 0;
`